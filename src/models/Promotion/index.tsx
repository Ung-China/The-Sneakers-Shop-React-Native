export default class Promotion {
  id: number;
  title: string;
  description: number;

  constructor(id: number, title: string, description: number) {
    this.id = id;
    this.title = title;
    this.description = description;
  }
}
